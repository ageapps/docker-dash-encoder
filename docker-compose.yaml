version: '3'
services:
  encoder:
    build: .
    image: docker-dash-encoder
    volumes:
    - './convert.sh:/media/convert.sh'
    - './data/out:/media/out'
    - './data/in:/media/in'
    command: bash ./convert.sh ./in/InMovie.mov
