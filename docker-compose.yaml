version: '3'
services:
  encoder:
    build: .
    image: docker-dash-encoder
    volumes:
    - './convert.sh:/media/convert.sh'
    - './media/in:/media/in'
    command: bash ./convert.sh ./in/pepe.mp4
